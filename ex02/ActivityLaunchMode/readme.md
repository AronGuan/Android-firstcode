#活动的启动模式  
1. standard:每次启动都会创建该活动的一个新的实例。  
2. singleTop:在启动活动时，如果发现返回栈的栈顶已经是该活动，就直接使用它，不会创建新的活动，如果不在栈顶，则会创建新的实例。  
3. singleTask:每次启动，检查返回栈是否存在该活动的实例，如果有，那么把这个活动上所有活动统统出栈(销毁),使其处在栈顶。  
4. singleInstance:单例模式类似，通过新建一个返回栈来存放此活动，这样，哪个应用来访问这个活动，都共用同一个返回栈，解决共享活动实例的问题。
